#!/usr/bin/env bash

CURRENT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

#tmux bind-key T run-shell

inter_="\#{devterm_temp}"

main() {
#	update_tmux_option "status-right"
	devterm_voltage=$(echo $(sh "$CURRENT_DIR/scripts/tmux-devterm-voltage.sh"))
#	local interpolate="${$devterm_voltage/$inter_}"
#	tmux set-option -gq "$inter_" "$devterm_voltage"

	echo $devterm_voltage
}
main

